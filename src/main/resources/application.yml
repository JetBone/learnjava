spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/demo?useUnicodxe=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true
    username: demo
    password: demo123
  redis:
    host: localhost
#  security:
#    user:
#      name: admin
#      password: 123456
#      roles: amdin,manager,customer

mybatis:
  mapper-locations: classpath:mapper/*.xml

logging:
  level:
    com.jetbone.app.mapper: debug

tmp.dir: ~/tmp
